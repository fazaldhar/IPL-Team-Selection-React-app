import React from "React";

const Header = (props) => {
	return (
		<header className="header">
			<div className="text-center">
				<h1>Online IPL Team Selection</h1>
			</div>
		</header>
	);
};

export default Header;